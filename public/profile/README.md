Upload your profile photo here.

Suggested path:

```ts
const profilePhotoSrc = './profile/profile-photo.jpg'
```

After uploading the image, paste that path into `profilePhotoSrc` in `src/components/Hero.tsx`.
